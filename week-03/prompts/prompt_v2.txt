Classify the ticket into one of: billing, technical, account, shipping. Return JSON {category, rationale}. Classify by the primary intent, not incidental words: if money is only context for a delivery problem, choose shipping.